rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /days/{dayId} {
      // Hanya user yang sudah login (via Firebase Auth) bisa baca & tulis
      allow read, write: if request.auth != null;
    }
  }
}
